FROM xychelsea/ffmpeg-nvidia:latest
LABEL description="Video AI System - Base Ready"

# Instalar apenas Jupyter Lab (mais simples)
USER anaconda
RUN pip install jupyterlab

# Comando simples para manter rodando
CMD ["sleep", "infinity"]
